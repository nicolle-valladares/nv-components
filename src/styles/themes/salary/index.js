import {
  BRAND_COLORS,
  BRAND_SHADES,
  BRAND_TINTS,
  COLORS,
  ELEVATIONS,
  SHADOWS,
  TRANSLUCENT_COLORS
} from './color'
import { FONTS, TYPOGRAPHY } from './typography'

export const salaryTheme = Object.seal({
  brandColors: BRAND_COLORS,
  colors: COLORS,
  elevations: ELEVATIONS,
  shades: BRAND_SHADES,
  shadows: SHADOWS,
  tints: BRAND_TINTS,
  translucent: TRANSLUCENT_COLORS,
  typography: TYPOGRAPHY,
  fonts: FONTS
})
